const INITIAL_STATE = {
    username: null,
    isLoggedIn: true,
};

export const authReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
