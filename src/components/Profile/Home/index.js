import React from "react";
import Info from "./Info";
const Home = (props) => {
    return (
        <div>
            <Info />
            {/* Add Tabs for nested routing */}
        </div>
    );
};
export default Home;
